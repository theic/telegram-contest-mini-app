import { ChunkEntity } from './chunk.entity';
import { ProjectEntity } from './project.entity';

export const entities = [
  ChunkEntity,
  ProjectEntity
];

export {
  ChunkEntity,
  ProjectEntity,
};
